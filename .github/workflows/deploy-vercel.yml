# Deploy manejado directamente por la integración Vercel ↔ GitHub.
# Este workflow está desactivado para evitar duplicación.
# Vercel detecta los pushes a main automáticamente y deployea.
name: Deploy Frontend → Vercel (desactivado)

on:
  workflow_dispatch: # solo manual, no automático
