/**
 * Identidad del producto — un solo lugar.
 *
 * Ojo con la distinción: esto es la marca del PRODUCTO (lo que ve alguien que
 * todavía no es cliente, o que está fuera de una empresa). El nombre que ve un
 * usuario adentro del backoffice es el de SU empresa, y sale de `companies`
 * vía CompanyContext — no de acá.
 */
export const PRODUCT_NAME = 'Bocha';

/** Bajada corta, para metadata y encabezados. */
export const PRODUCT_TAGLINE = 'Backoffice para PyMEs';

/** Contacto comercial y de soporte (hoy el mismo). */
export const CONTACT_EMAIL = 'carlosaureliorodriguez23@gmail.com';
